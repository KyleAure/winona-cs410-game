# RSA Modeling Software
Use the IBM RSA Modeling Software to develop use case models.
Save the Model Package in this directory (/rsa) as a .emx file.
Export models the (/images) directory as a .png image file.
